{
  "name": "@connectors/core",
  "version": "0.1.0",
  "description": "Shared contracts for connectors: lifecycle, events, storage, publishing, errors, logging",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/talitman/connectors.git",
    "directory": "packages/core"
  },
  "homepage": "https://github.com/talitman/connectors/tree/main/packages/core#readme",
  "bugs": "https://github.com/talitman/connectors/issues",
  "keywords": [
    "connectors",
    "integration"
  ],
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  }
}
